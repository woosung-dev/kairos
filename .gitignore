# Dependencies
node_modules/
.pnpm-store/

# Next.js
apps/web/.next/
apps/web/out/

# Environment
.env
.env.local
.env.*.local

# QA 자격증명 (Multi-Agent QA Sprint 18~ — JWT/password 절대 commit 금지)
**/seed-credentials.env
apps/api/scripts/seed-credentials.env
docs/dev-log/**/seed-credentials.env

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Sprint 15 Day 0 audio samples (개인 음성 데이터 — README만 commit)
apps/api/scripts/samples/*.webm
apps/api/scripts/samples/*.mp4
apps/api/scripts/samples/*.wav
apps/api/scripts/samples/*.aac
apps/api/scripts/samples/*.m4a

# Build
dist/
build/

# Python
__pycache__/
*.pyc
.venv/
.env/

# Debug
npm-debug.log*
pnpm-debug.log*

# Misc
*.tsbuildinfo
next-env.d.ts

# Playwright MCP
.playwright-mcp/
e2e-*.png

# MCP config
.mcp.json

# AI 에이전트 로컬 설정
# .ai/ 는 2026-08-15 ADR-029 로 해체됐다 (규칙은 apps/*/AGENTS.md 로 이전).
# ★재도입 금지 — 규칙을 gitignore 밖에 두면 정본이 다시 git 밖으로 나간다. 이 줄은 재발 방지용으로 남긴다.
.ai/
.claude/

# gstack
skills-lock.json
.agents/
.gstack/
.context/

# Python coverage artifact
apps/api/.coverage

# OCI 배포 빌드 인자 (ADR-028) — 값이 채워진 실제 파일
deploy/oci/build.env
