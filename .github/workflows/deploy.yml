name: Deploy Backend (Cloud Run)

on:
  push:
    branches: [main]
    paths:
      - "backend/**"
      - ".github/workflows/deploy.yml"
  workflow_dispatch:

concurrency:
  group: deploy-backend
  cancel-in-progress: false

env:
  GCP_PROJECT: woosung-dev
  GCP_REGION: asia-northeast3
  SERVICE_NAME: kairos-api
  IMAGE_NAME: ghcr.io/${{ github.repository }}/api

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write  # WIF OIDC 토큰 발급용
      packages: write  # GHCR push용

    steps:
      - uses: actions/checkout@v4

      # ── 이미지 빌드 + GHCR push ──────────────────────────────────────────
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push image to GHCR
        working-directory: backend
        run: |
          set -euo pipefail
          SHA_TAG="${{ env.IMAGE_NAME }}:${{ github.sha }}"
          LATEST_TAG="${{ env.IMAGE_NAME }}:latest"

          docker buildx build \
            --platform linux/amd64 \
            --tag "$SHA_TAG" \
            --tag "$LATEST_TAG" \
            --push \
            .

          echo "IMAGE_URI=$SHA_TAG" >> "$GITHUB_ENV"

      # ── GCP 인증 (WIF — 키 파일 없음) ────────────────────────────────────
      - name: Authenticate to Google Cloud (WIF)
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOYER_SA }}

      - name: Setup gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Deploy to Cloud Run
        run: |
          set -euo pipefail
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image "${{ env.IMAGE_URI }}" \
            --region ${{ env.GCP_REGION }} \
            --project ${{ env.GCP_PROJECT }} \
            --port 8000 \
            --memory 1Gi \
            --min-instances 0 \
            --max-instances 3 \
            --allow-unauthenticated \
            --set-env-vars="^|^APP_ENV=production|LOG_LEVEL=INFO|CORS_ORIGINS=${{ secrets.CORS_ORIGINS }}|FRONTEND_URL=${{ secrets.FRONTEND_URL }}|DATABASE_URL=${{ secrets.DATABASE_URL }}|CLERK_SECRET_KEY=${{ secrets.CLERK_SECRET_KEY }}|CLERK_WEBHOOK_SECRET=${{ secrets.CLERK_WEBHOOK_SECRET }}|R2_ACCOUNT_ID=${{ secrets.R2_ACCOUNT_ID }}|R2_ACCESS_KEY_ID=${{ secrets.R2_ACCESS_KEY_ID }}|R2_SECRET_ACCESS_KEY=${{ secrets.R2_SECRET_ACCESS_KEY }}|R2_BUCKET_NAME=${{ secrets.R2_BUCKET_NAME }}|GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}|OPENAI_API_KEY=${{ secrets.OPENAI_API_KEY }}" \
            --quiet

      - name: Smoke test
        run: |
          set -euo pipefail
          URL=$(gcloud run services describe ${{ env.SERVICE_NAME }} \
            --region ${{ env.GCP_REGION }} \
            --format="value(status.url)")
          for i in 1 2 3 4 5; do
            STATUS=$(curl -s -o /dev/null -w "%{http_code}" "${URL}/api/v1/docs")
            if [ "$STATUS" = "200" ]; then
              echo "✅ $URL/api/v1/docs → 200 (attempt $i)"
              exit 0
            fi
            echo "⏳ attempt $i: got $STATUS, retrying in 10s"
            sleep 10
          done
          echo "❌ Smoke test failed after 5 attempts"
          exit 1
