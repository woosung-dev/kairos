# Sprint 2 구현 계획: Inbox + 프로젝트 연결 + 액션 + 반응형

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 회의 업로드 → AI 요약 + 액션 추출 → Inbox 적재 → 프로젝트 연결 전체 E2E 체인 완성 + 반응형 3단계 + m4a 호환

**Architecture:** Vertical Slice 4개 — (1) Project BE+FE, (2) ActionItem BE+FE+칸반, (3) Inbox+AI 파이프라인 확장, (4) 인프라(ffmpeg+반응형). Slice 4는 별도 worktree에서 병렬 실행.

**Tech Stack:** FastAPI + SQLModel + Alembic (BE) / Next.js 16 + React Query + @dnd-kit + Zustand (FE) / Gemini 2.5 Flash (AI)

**참조 규칙:**
- `.ai/rules/backend.md` — Router/Service/Repository 패턴, 100% Async, Pydantic V2
- `.ai/rules/frontend.md` — FSD Lite, React Query, Zod v4, shadcn/ui v4
- `.ai/rules/typescript.md` — Strict, any 금지, 네이밍

**설계 문서:** `docs/superpowers/specs/2026-04-02-sprint2-design.md`

---

## 파일 구조 맵

### 백엔드 — 신규 생성

```
backend/src/
├── projects/
│   ├── models.py              # Project, MeetingProjectLink (SQLModel)
│   ├── schemas.py             # ProjectCreate, ProjectUpdate, ProjectResponse
│   ├── repository.py          # CRUD + MeetingProjectLink 관리
│   ├── service.py             # archive 로직
│   ├── router.py              # 6개 엔드포인트 + 2개 (MeetingProject)
│   ├── dependencies.py        # Depends() 조립
│   └── exceptions.py          # ProjectNotFoundError
├── actions/
│   ├── models.py              # ActionItem (SQLModel)
│   ├── schemas.py             # ActionItemCreate, ActionItemUpdate
│   ├── repository.py          # CRUD + 필터
│   ├── service.py
│   ├── router.py              # 3개 엔드포인트
│   ├── dependencies.py
│   └── exceptions.py
├── inbox/
│   ├── models.py              # InboxItem (SQLModel)
│   ├── schemas.py             # ClassifyRequest, InboxItemResponse
│   ├── repository.py          # CRUD
│   ├── service.py             # classify, dismiss 로직
│   ├── router.py              # 3개 엔드포인트
│   ├── dependencies.py
│   └── exceptions.py
```

### 백엔드 — 수정

```
backend/src/
├── main.py                          # 라우터 등록 추가
├── common/prompts.py                # MEETING_ACTIONS_AND_LINKING 프롬프트 추가
├── services/ai_processing.py        # extract_actions_and_link() 메서드 추가
├── services/transcription.py        # convert_to_wav() ffmpeg 변환 추가
├── meetings/pipeline_service.py     # 파이프라인 확장 (액션+Inbox+자동확정)
├── meetings/dependencies.py         # pipeline에 신규 repo 주입
├── meetings/models.py               # status enum에서 "embedding" 제거
```

### 백엔드 — 마이그레이션

```
backend/alembic/versions/
└── xxxx_add_sprint2_tables.py       # Project, ActionItem, InboxItem, MeetingProjectLink
```

### 프론트엔드 — 신규 생성

```
frontend/src/
├── features/projects/
│   ├── api.ts                       # projectKeys + CRUD 함수
│   ├── hooks.ts                     # useProjects, useCreateProject, useUpdateProject...
│   └── schemas.ts                   # Zod v4 스키마
├── features/actions/
│   ├── api.ts                       # actionKeys + CRUD 함수
│   ├── hooks.ts                     # useActionItems, useUpdateActionItem
│   ├── schemas.ts
│   └── components/action-kanban.tsx # dnd-kit 재구현
├── features/inbox/
│   ├── api.ts                       # inboxKeys + classify/dismiss 함수
│   ├── hooks.ts                     # useInbox, useClassifyInbox, useDismissInbox, useInboxAutoClassifyToast
│   └── schemas.ts
├── features/projects/components/
│   └── project-combobox.tsx         # 공통 프로젝트 선택 Combobox
├── components/layout/
│   └── bottom-nav.tsx               # 모바일 하단 네비게이션
├── hooks/
│   └── use-media-query.ts           # breakpoint 감지 훅
```

### 프론트엔드 — 수정

```
frontend/src/
├── features/projects/types.ts              # category 제거, tags 추가
├── features/projects/components/
│   ├── project-list.tsx                    # mock → hook 교체
│   ├── project-card.tsx                    # category 제거
│   └── project-detail.tsx                  # hook 연결 + 액션 탭
├── features/actions/types.ts               # 기존 유지
├── features/actions/components/
│   └── action-list.tsx                     # hook 연결
├── features/inbox/types.ts                 # PARA → project 필드 변경
├── features/inbox/components/
│   ├── inbox-list.tsx                      # hook 연결
│   └── inbox-item-card.tsx                 # 프로젝트 연결 UI
├── features/meetings/types.ts              # status에서 "embedding" 제거
├── features/meetings/components/
│   └── meeting-detail.tsx                  # 프로젝트 연결 섹션 추가
├── features/upload/hooks.ts                # 진행률 UI 개선
├── store/ui.ts                             # sidebarCollapsed, isMobile 추가
├── components/layout/panel-layout.tsx      # 반응형 3단계
├── components/layout/sidebar.tsx           # 아이콘 모드
├── app/globals.css                         # 반응형 CSS 변수
├── app/(app)/layout.tsx                    # 하단 nav 추가
```

### 문서 — 수정

```
DESIGN.md                                   # 반응형 breakpoint 추가
.ai/rules/frontend.md                       # 반응형 규칙 추가
docs/api/endpoints.md                       # PARA 제거 반영 + MeetingProject API
docs/architecture/erd.md                    # category 제거 반영
```

---

## Slice 1: Project BE + FE

### Task 1: DB 마이그레이션 — Sprint 2 테이블 생성

**Files:**
- Create: `backend/src/projects/models.py`
- Create: `backend/src/actions/models.py`
- Create: `backend/src/inbox/models.py`
- Create: `backend/alembic/versions/xxxx_add_sprint2_tables.py` (autogenerate)
- Modify: `backend/src/main.py` (모델 import 추가)

- [ ] **Step 1: Project + MeetingProjectLink 모델 정의**

```python
# backend/src/projects/__init__.py
# (빈 파일)

# backend/src/projects/models.py
import uuid
from datetime import datetime

from sqlalchemy import JSON, ForeignKey, UniqueConstraint
from sqlmodel import Field, SQLModel


class Project(SQLModel, table=True):
    __tablename__ = "projects"

    id: uuid.UUID = Field(default_factory=uuid.uuid4, primary_key=True)
    workspace_id: uuid.UUID = Field(foreign_key="workspaces.id")
    title: str
    description: str | None = None
    status: str = "active"  # active | completed | archived
    tags: list[str] = Field(default_factory=list, sa_type=JSON)
    sort_order: int = 0
    created_by_id: uuid.UUID = Field(foreign_key="users.id")
    created_at: datetime = Field(default_factory=datetime.utcnow)
    updated_at: datetime = Field(default_factory=datetime.utcnow)


class MeetingProjectLink(SQLModel, table=True):
    __tablename__ = "meeting_project_links"
    __table_args__ = (
        UniqueConstraint("meeting_id", "project_id", name="uq_meeting_project"),
    )

    id: uuid.UUID = Field(default_factory=uuid.uuid4, primary_key=True)
    meeting_id: uuid.UUID = Field(foreign_key="meetings.id")
    project_id: uuid.UUID = Field(foreign_key="projects.id")
```

- [ ] **Step 2: ActionItem 모델 정의**

```python
# backend/src/actions/__init__.py
# (빈 파일)

# backend/src/actions/models.py
import uuid
from datetime import date, datetime

from sqlmodel import Field, SQLModel


class ActionItem(SQLModel, table=True):
    __tablename__ = "action_items"

    id: uuid.UUID = Field(default_factory=uuid.uuid4, primary_key=True)
    workspace_id: uuid.UUID = Field(foreign_key="workspaces.id")
    meeting_id: uuid.UUID | None = Field(default=None, foreign_key="meetings.id")
    project_id: uuid.UUID | None = Field(default=None, foreign_key="projects.id")
    title: str
    description: str | None = None
    assignee_id: uuid.UUID | None = Field(default=None, foreign_key="users.id")
    due_date: date | None = None
    priority: str = "medium"  # high | medium | low
    status: str = "todo"  # todo | in_progress | done | cancelled
    created_at: datetime = Field(default_factory=datetime.utcnow)
    updated_at: datetime = Field(default_factory=datetime.utcnow)
```

- [ ] **Step 3: InboxItem 모델 정의**

```python
# backend/src/inbox/__init__.py
# (빈 파일)

# backend/src/inbox/models.py
import uuid
from datetime import datetime

from sqlalchemy import JSON
from sqlmodel import Field, SQLModel


class InboxItem(SQLModel, table=True):
    __tablename__ = "inbox_items"

    id: uuid.UUID = Field(default_factory=uuid.uuid4, primary_key=True)
    workspace_id: uuid.UUID = Field(foreign_key="workspaces.id")
    title: str
    summary: str | None = None
    source_type: str  # meeting | note | attachment
    source_id: uuid.UUID
    ai_suggested_project_id: uuid.UUID | None = Field(
        default=None, foreign_key="projects.id"
    )
    ai_suggested_project_title: str | None = None
    ai_suggested_tags: list[str] = Field(default_factory=list, sa_type=JSON)
    ai_confidence: float = 0.0
    is_processed: bool = False
    created_at: datetime = Field(default_factory=datetime.utcnow)
    updated_at: datetime = Field(default_factory=datetime.utcnow)
```

- [ ] **Step 4: Alembic 마이그레이션 생성 및 적용**

```bash
cd /Users/woosung/project/agy-project/kairos/backend
# main.py 또는 alembic env.py에서 신규 모델 import 확인 후:
alembic revision --autogenerate -m "add sprint2 tables: projects, action_items, inbox_items, meeting_project_links"
alembic upgrade head
```

Expected: 4개 테이블 생성 완료

- [ ] **Step 5: 커밋**

```bash
git add backend/src/projects/ backend/src/actions/ backend/src/inbox/ backend/alembic/
git commit -m "feat: Sprint 2 DB 모델 + 마이그레이션 (Project, ActionItem, InboxItem, MeetingProjectLink)"
```

---

### Task 2: Project BE — Repository + Service + Dependencies

**Files:**
- Create: `backend/src/projects/repository.py`
- Create: `backend/src/projects/service.py`
- Create: `backend/src/projects/schemas.py`
- Create: `backend/src/projects/dependencies.py`
- Create: `backend/src/projects/exceptions.py`

- [ ] **Step 1: Project 예외 정의**

```python
# backend/src/projects/exceptions.py
from src.common.exceptions import NotFoundError


class ProjectNotFoundError(NotFoundError):
    def __init__(self) -> None:
        super().__init__("프로젝트")
```

- [ ] **Step 2: Project 스키마 정의**

```python
# backend/src/projects/schemas.py
from pydantic import BaseModel, Field


class CreateProjectRequest(BaseModel):
    title: str
    description: str | None = None
    tags: list[str] = Field(default_factory=list)

    model_config = {"populate_by_name": True}


class UpdateProjectRequest(BaseModel):
    title: str | None = None
    description: str | None = None
    status: str | None = None
    tags: list[str] | None = None

    model_config = {"populate_by_name": True}


class ArchiveProjectRequest(BaseModel):
    pass  # 단순 status 전환, body 불필요
```

- [ ] **Step 3: Project Repository 구현**

```python
# backend/src/projects/repository.py
import uuid

from sqlalchemy import func, select
from sqlalchemy.ext.asyncio import AsyncSession

from src.projects.models import MeetingProjectLink, Project


class ProjectRepository:
    def __init__(self, session: AsyncSession) -> None:
        self.session = session

    async def find_by_id(self, project_id: uuid.UUID) -> Project | None:
        result = await self.session.execute(
            select(Project).where(Project.id == project_id)
        )
        return result.scalar_one_or_none()

    async def find_by_workspace(
        self,
        workspace_id: uuid.UUID,
        status: str | None = None,
        tag: str | None = None,
        offset: int = 0,
        limit: int = 50,
    ) -> list[Project]:
        stmt = select(Project).where(Project.workspace_id == workspace_id)
        if status:
            stmt = stmt.where(Project.status == status)
        # 태그 필터: JSONB contains 연산
        if tag:
            stmt = stmt.where(Project.tags.contains([tag]))
        stmt = stmt.order_by(Project.sort_order, Project.created_at.desc())
        stmt = stmt.offset(offset).limit(limit)
        result = await self.session.execute(stmt)
        return list(result.scalars().all())

    async def count_by_workspace(
        self, workspace_id: uuid.UUID, status: str | None = None
    ) -> int:
        stmt = select(func.count()).select_from(Project).where(
            Project.workspace_id == workspace_id
        )
        if status:
            stmt = stmt.where(Project.status == status)
        result = await self.session.execute(stmt)
        return result.scalar_one()

    async def save(self, project: Project) -> Project:
        self.session.add(project)
        await self.session.flush()
        return project

    async def delete(self, project: Project) -> None:
        await self.session.delete(project)
        await self.session.flush()

    async def commit(self) -> None:
        await self.session.commit()

    # --- MeetingProjectLink ---
    async def add_meeting_link(
        self, meeting_id: uuid.UUID, project_id: uuid.UUID
    ) -> MeetingProjectLink:
        link = MeetingProjectLink(meeting_id=meeting_id, project_id=project_id)
        self.session.add(link)
        await self.session.flush()
        return link

    async def remove_meeting_link(
        self, meeting_id: uuid.UUID, project_id: uuid.UUID
    ) -> None:
        result = await self.session.execute(
            select(MeetingProjectLink).where(
                MeetingProjectLink.meeting_id == meeting_id,
                MeetingProjectLink.project_id == project_id,
            )
        )
        link = result.scalar_one_or_none()
        if link:
            await self.session.delete(link)
            await self.session.flush()

    async def find_projects_by_meeting(
        self, meeting_id: uuid.UUID
    ) -> list[Project]:
        result = await self.session.execute(
            select(Project)
            .join(MeetingProjectLink, MeetingProjectLink.project_id == Project.id)
            .where(MeetingProjectLink.meeting_id == meeting_id)
        )
        return list(result.scalars().all())
```

- [ ] **Step 4: Project Service 구현**

```python
# backend/src/projects/service.py
import uuid
from datetime import datetime

from src.projects.exceptions import ProjectNotFoundError
from src.projects.models import Project
from src.projects.repository import ProjectRepository


class ProjectService:
    def __init__(self, repo: ProjectRepository) -> None:
        self.repo = repo

    async def create_project(
        self,
        workspace_id: uuid.UUID,
        title: str,
        created_by_id: uuid.UUID,
        description: str | None = None,
        tags: list[str] | None = None,
    ) -> dict:
        project = Project(
            workspace_id=workspace_id,
            title=title,
            description=description,
            tags=tags or [],
            created_by_id=created_by_id,
        )
        project = await self.repo.save(project)
        await self.repo.commit()
        return self._to_dict(project)

    async def list_projects(
        self,
        workspace_id: uuid.UUID,
        status: str | None = None,
        tag: str | None = None,
        page: int = 1,
        page_size: int = 50,
    ) -> dict:
        offset = (page - 1) * page_size
        projects = await self.repo.find_by_workspace(
            workspace_id, status=status, tag=tag, offset=offset, limit=page_size
        )
        total = await self.repo.count_by_workspace(workspace_id, status=status)
        return {
            "items": [self._to_dict(p) for p in projects],
            "total": total,
            "page": page,
            "pageSize": page_size,
            "hasNext": page * page_size < total,
        }

    async def get_project(self, project_id: uuid.UUID) -> dict:
        project = await self.repo.find_by_id(project_id)
        if project is None:
            raise ProjectNotFoundError()
        return self._to_dict(project)

    async def update_project(
        self,
        project_id: uuid.UUID,
        title: str | None = None,
        description: str | None = None,
        status: str | None = None,
        tags: list[str] | None = None,
    ) -> dict:
        project = await self.repo.find_by_id(project_id)
        if project is None:
            raise ProjectNotFoundError()
        if title is not None:
            project.title = title
        if description is not None:
            project.description = description
        if status is not None:
            project.status = status
        if tags is not None:
            project.tags = tags
        project.updated_at = datetime.utcnow()
        await self.repo.save(project)
        await self.repo.commit()
        return self._to_dict(project)

    async def delete_project(self, project_id: uuid.UUID) -> None:
        project = await self.repo.find_by_id(project_id)
        if project is None:
            raise ProjectNotFoundError()
        await self.repo.delete(project)
        await self.repo.commit()

    async def archive_project(self, project_id: uuid.UUID) -> dict:
        project = await self.repo.find_by_id(project_id)
        if project is None:
            raise ProjectNotFoundError()
        project.status = "archived"
        project.updated_at = datetime.utcnow()
        await self.repo.save(project)
        await self.repo.commit()
        return {"id": str(project.id), "status": project.status}

    # --- Meeting-Project 연결 ---
    async def add_meeting_project(
        self, meeting_id: uuid.UUID, project_id: uuid.UUID
    ) -> dict:
        project = await self.repo.find_by_id(project_id)
        if project is None:
            raise ProjectNotFoundError()
        link = await self.repo.add_meeting_link(meeting_id, project_id)
        await self.repo.commit()
        return {"id": str(link.id), "meetingId": str(meeting_id), "projectId": str(project_id)}

    async def remove_meeting_project(
        self, meeting_id: uuid.UUID, project_id: uuid.UUID
    ) -> None:
        await self.repo.remove_meeting_link(meeting_id, project_id)
        await self.repo.commit()

    async def get_meeting_projects(self, meeting_id: uuid.UUID) -> list[dict]:
        projects = await self.repo.find_projects_by_meeting(meeting_id)
        return [self._to_dict(p) for p in projects]

    @staticmethod
    def _to_dict(project: Project) -> dict:
        return {
            "id": str(project.id),
            "workspaceId": str(project.workspace_id),
            "title": project.title,
            "description": project.description,
            "status": project.status,
            "tags": project.tags,
            "sortOrder": project.sort_order,
            "createdAt": project.created_at.isoformat(),
            "updatedAt": project.updated_at.isoformat(),
        }
```

- [ ] **Step 5: Project Dependencies 구현**

```python
# backend/src/projects/dependencies.py
from fastapi import Depends
from sqlalchemy.ext.asyncio import AsyncSession

from src.common.database import get_async_session
from src.projects.repository import ProjectRepository
from src.projects.service import ProjectService


async def get_project_repository(
    session: AsyncSession = Depends(get_async_session),
) -> ProjectRepository:
    return ProjectRepository(session)


async def get_project_service(
    repo: ProjectRepository = Depends(get_project_repository),
) -> ProjectService:
    return ProjectService(repo)
```

- [ ] **Step 6: 커밋**

```bash
git add backend/src/projects/
git commit -m "feat: Project BE — Repository + Service + Dependencies"
```

---

### Task 3: Project BE — Router 등록

**Files:**
- Create: `backend/src/projects/router.py`
- Modify: `backend/src/main.py`

- [ ] **Step 1: Project Router 구현 (8개 엔드포인트)**

```python
# backend/src/projects/router.py
import uuid

from fastapi import APIRouter, Depends, Query

from src.auth.dependencies import get_current_user
from src.auth.models import User
from src.projects.dependencies import get_project_service
from src.projects.schemas import CreateProjectRequest, UpdateProjectRequest
from src.projects.service import ProjectService

router = APIRouter(
    prefix="/api/v1/workspaces/{workspace_id}/projects",
    tags=["projects"],
)


@router.get("")
async def list_projects(
    workspace_id: uuid.UUID,
    status: str | None = None,
    tag: str | None = None,
    page: int = Query(default=1, ge=1),
    page_size: int = Query(default=50, ge=1, le=100, alias="pageSize"),
    current_user: User = Depends(get_current_user),
    service: ProjectService = Depends(get_project_service),
):
    return await service.list_projects(workspace_id, status=status, tag=tag, page=page, page_size=page_size)


@router.get("/{project_id}")
async def get_project(
    workspace_id: uuid.UUID,
    project_id: uuid.UUID,
    current_user: User = Depends(get_current_user),
    service: ProjectService = Depends(get_project_service),
):
    return await service.get_project(project_id)


@router.post("", status_code=201)
async def create_project(
    workspace_id: uuid.UUID,
    data: CreateProjectRequest,
    current_user: User = Depends(get_current_user),
    service: ProjectService = Depends(get_project_service),
):
    return await service.create_project(
        workspace_id=workspace_id,
        title=data.title,
        description=data.description,
        tags=data.tags,
        created_by_id=current_user.id,
    )


@router.patch("/{project_id}")
async def update_project(
    workspace_id: uuid.UUID,
    project_id: uuid.UUID,
    data: UpdateProjectRequest,
    current_user: User = Depends(get_current_user),
    service: ProjectService = Depends(get_project_service),
):
    return await service.update_project(
        project_id=project_id,
        title=data.title,
        description=data.description,
        status=data.status,
        tags=data.tags,
    )


@router.delete("/{project_id}", status_code=204)
async def delete_project(
    workspace_id: uuid.UUID,
    project_id: uuid.UUID,
    current_user: User = Depends(get_current_user),
    service: ProjectService = Depends(get_project_service),
):
    await service.delete_project(project_id)


@router.post("/{project_id}/archive")
async def archive_project(
    workspace_id: uuid.UUID,
    project_id: uuid.UUID,
    current_user: User = Depends(get_current_user),
    service: ProjectService = Depends(get_project_service),
):
    return await service.archive_project(project_id)


# --- Meeting-Project 연결 ---

meeting_project_router = APIRouter(
    prefix="/api/v1/workspaces/{workspace_id}/meetings/{meeting_id}/projects",
    tags=["meeting-projects"],
)


@meeting_project_router.post("", status_code=201)
async def add_meeting_project(
    workspace_id: uuid.UUID,
    meeting_id: uuid.UUID,
    data: dict,  # { "projectId": "uuid" }
    current_user: User = Depends(get_current_user),
    service: ProjectService = Depends(get_project_service),
):
    project_id = uuid.UUID(data["projectId"])
    return await service.add_meeting_project(meeting_id, project_id)


@meeting_project_router.delete("/{project_id}", status_code=204)
async def remove_meeting_project(
    workspace_id: uuid.UUID,
    meeting_id: uuid.UUID,
    project_id: uuid.UUID,
    current_user: User = Depends(get_current_user),
    service: ProjectService = Depends(get_project_service),
):
    await service.remove_meeting_project(meeting_id, project_id)
```

- [ ] **Step 2: main.py에 라우터 등록**

`backend/src/main.py`에 추가:

```python
from src.projects.router import router as projects_router
from src.projects.router import meeting_project_router

app.include_router(projects_router)
app.include_router(meeting_project_router)
```

- [ ] **Step 3: 서버 기동 테스트**

```bash
cd /Users/woosung/project/agy-project/kairos/backend
uv run uvicorn src.main:app --reload --port 8000
# /api/v1/docs 에서 projects 엔드포인트 확인
```

- [ ] **Step 4: 커밋**

```bash
git add backend/src/projects/router.py backend/src/main.py
git commit -m "feat: Project BE — Router 8개 엔드포인트 등록"
```

---

### Task 4: Project FE — API 레이어 + Hook

**Files:**
- Create: `frontend/src/features/projects/api.ts`
- Create: `frontend/src/features/projects/hooks.ts`
- Create: `frontend/src/features/projects/schemas.ts`
- Modify: `frontend/src/features/projects/types.ts`

- [ ] **Step 1: Project 타입 수정 (PARA 제거)**

`frontend/src/features/projects/types.ts`에서 `category` 관련 필드를 제거하고 `tags` 추가:

```typescript
import type { UUID, Timestamped } from "@/types";

export type ProjectStatus = "active" | "completed" | "archived";

export interface Project extends Timestamped {
  id: UUID;
  workspaceId: UUID;
  title: string;
  description: string | null;
  status: ProjectStatus;
  tags: string[];
  sortOrder: number;
}

export interface CreateProjectRequest {
  title: string;
  description?: string | null;
  tags?: string[];
}

export interface UpdateProjectRequest {
  title?: string;
  description?: string | null;
  status?: ProjectStatus;
  tags?: string[];
}
```

- [ ] **Step 2: Project API + Query Key Factory**

```typescript
// frontend/src/features/projects/api.ts
import { apiClient } from "@/lib/api-client";
import type { PaginatedResponse } from "@/types";
import type { CreateProjectRequest, Project, UpdateProjectRequest } from "./types";

export const projectKeys = {
  all: ["projects"] as const,
  list: (wid: string) => [...projectKeys.all, "list", wid] as const,
  detail: (wid: string, id: string) =>
    [...projectKeys.all, "detail", wid, id] as const,
};

export async function fetchProjects(
  token: string,
  wid: string,
  params?: { status?: string; tag?: string; page?: number }
): Promise<PaginatedResponse<Project>> {
  const query = new URLSearchParams();
  if (params?.status) query.set("status", params.status);
  if (params?.tag) query.set("tag", params.tag);
  if (params?.page) query.set("page", String(params.page));
  const qs = query.toString();
  return apiClient<PaginatedResponse<Project>>(
    `/workspaces/${wid}/projects${qs ? `?${qs}` : ""}`,
    { token }
  );
}

export async function fetchProject(
  token: string,
  wid: string,
  id: string
): Promise<Project> {
  return apiClient<Project>(`/workspaces/${wid}/projects/${id}`, { token });
}

export async function createProject(
  token: string,
  wid: string,
  data: CreateProjectRequest
): Promise<Project> {
  return apiClient<Project>(`/workspaces/${wid}/projects`, {
    token,
    method: "POST",
    body: JSON.stringify(data),
  });
}

export async function updateProject(
  token: string,
  wid: string,
  id: string,
  data: UpdateProjectRequest
): Promise<Project> {
  return apiClient<Project>(`/workspaces/${wid}/projects/${id}`, {
    token,
    method: "PATCH",
    body: JSON.stringify(data),
  });
}

export async function deleteProject(
  token: string,
  wid: string,
  id: string
): Promise<void> {
  return apiClient<void>(`/workspaces/${wid}/projects/${id}`, {
    token,
    method: "DELETE",
  });
}

export async function archiveProject(
  token: string,
  wid: string,
  id: string
): Promise<{ id: string; status: string }> {
  return apiClient(`/workspaces/${wid}/projects/${id}/archive`, {
    token,
    method: "POST",
  });
}

// --- Meeting-Project 연결 ---
export async function addMeetingProject(
  token: string,
  wid: string,
  meetingId: string,
  projectId: string
): Promise<{ id: string; meetingId: string; projectId: string }> {
  return apiClient(`/workspaces/${wid}/meetings/${meetingId}/projects`, {
    token,
    method: "POST",
    body: JSON.stringify({ projectId }),
  });
}

export async function removeMeetingProject(
  token: string,
  wid: string,
  meetingId: string,
  projectId: string
): Promise<void> {
  return apiClient<void>(
    `/workspaces/${wid}/meetings/${meetingId}/projects/${projectId}`,
    { token, method: "DELETE" }
  );
}
```

- [ ] **Step 3: Project React Query Hooks**

```typescript
// frontend/src/features/projects/hooks.ts
"use client";

import { useAuth } from "@clerk/nextjs";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import {
  projectKeys,
  fetchProjects,
  fetchProject,
  createProject,
  updateProject,
  deleteProject,
  archiveProject,
  addMeetingProject,
  removeMeetingProject,
} from "./api";
import { meetingKeys } from "../meetings/api";
import type { CreateProjectRequest, UpdateProjectRequest } from "./types";

export function useProjects(
  wid: string | undefined,
  params?: { status?: string; tag?: string; page?: number }
) {
  const { getToken } = useAuth();
  return useQuery({
    queryKey: [...projectKeys.list(wid ?? ""), params],
    queryFn: async () => {
      const token = await getToken();
      if (!token) throw new Error("인증이 필요합니다");
      return fetchProjects(token, wid!, params);
    },
    enabled: !!wid,
  });
}

export function useProject(wid: string | undefined, id: string) {
  const { getToken } = useAuth();
  return useQuery({
    queryKey: projectKeys.detail(wid ?? "", id),
    queryFn: async () => {
      const token = await getToken();
      if (!token) throw new Error("인증이 필요합니다");
      return fetchProject(token, wid!, id);
    },
    enabled: !!wid,
  });
}

export function useCreateProject(wid: string | undefined) {
  const { getToken } = useAuth();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (data: CreateProjectRequest) => {
      const token = await getToken();
      if (!token) throw new Error("인증이 필요합니다");
      return createProject(token, wid!, data);
    },
    onSuccess: () => {
      if (wid) queryClient.invalidateQueries({ queryKey: projectKeys.list(wid) });
    },
  });
}

export function useUpdateProject(wid: string | undefined) {
  const { getToken } = useAuth();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async ({ id, data }: { id: string; data: UpdateProjectRequest }) => {
      const token = await getToken();
      if (!token) throw new Error("인증이 필요합니다");
      return updateProject(token, wid!, id, data);
    },
    onSuccess: () => {
      if (wid) queryClient.invalidateQueries({ queryKey: projectKeys.all });
    },
  });
}

export function useDeleteProject(wid: string | undefined) {
  const { getToken } = useAuth();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (id: string) => {
      const token = await getToken();
      if (!token) throw new Error("인증이 필요합니다");
      return deleteProject(token, wid!, id);
    },
    onSuccess: () => {
      if (wid) queryClient.invalidateQueries({ queryKey: projectKeys.list(wid) });
    },
  });
}

export function useArchiveProject(wid: string | undefined) {
  const { getToken } = useAuth();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (id: string) => {
      const token = await getToken();
      if (!token) throw new Error("인증이 필요합니다");
      return archiveProject(token, wid!, id);
    },
    onSuccess: () => {
      if (wid) queryClient.invalidateQueries({ queryKey: projectKeys.all });
    },
  });
}

export function useAddMeetingProject(wid: string | undefined) {
  const { getToken } = useAuth();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async ({ meetingId, projectId }: { meetingId: string; projectId: string }) => {
      const token = await getToken();
      if (!token) throw new Error("인증이 필요합니다");
      return addMeetingProject(token, wid!, meetingId, projectId);
    },
    onSuccess: (_data, variables) => {
      if (wid) {
        queryClient.invalidateQueries({ queryKey: meetingKeys.detail(wid, variables.meetingId) });
      }
    },
  });
}

export function useRemoveMeetingProject(wid: string | undefined) {
  const { getToken } = useAuth();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async ({ meetingId, projectId }: { meetingId: string; projectId: string }) => {
      const token = await getToken();
      if (!token) throw new Error("인증이 필요합니다");
      return removeMeetingProject(token, wid!, meetingId, projectId);
    },
    onSuccess: (_data, variables) => {
      if (wid) {
        queryClient.invalidateQueries({ queryKey: meetingKeys.detail(wid, variables.meetingId) });
      }
    },
  });
}
```

- [ ] **Step 4: Zod v4 스키마**

```typescript
// frontend/src/features/projects/schemas.ts
import { z } from "zod/v4";

export const createProjectSchema = z.object({
  title: z.string().min(1, "프로젝트 이름을 입력하세요"),
  description: z.string().optional(),
  tags: z.array(z.string()).default([]),
});

export type CreateProjectFormData = z.infer<typeof createProjectSchema>;
```

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/features/projects/
git commit -m "feat: Project FE — API 레이어 + React Query 훅 + Zod 스키마"
```

---

### Task 5: Project FE — 기존 컴포넌트 Mock→Real 전환

**Files:**
- Modify: `frontend/src/features/projects/components/project-list.tsx`
- Modify: `frontend/src/features/projects/components/project-card.tsx`
- Modify: `frontend/src/features/projects/components/project-detail.tsx`
- Modify: `frontend/src/components/layout/sidebar.tsx`

- [ ] **Step 1: project-card.tsx에서 category 제거, tags 표시로 변경**

기존 `category` 뱃지를 `tags` 뱃지로 교체. 기존 파일을 읽고 `category` 참조를 모두 제거, `tags` 배열을 `map`으로 렌더링.

- [ ] **Step 2: project-list.tsx를 mock에서 useProjects 훅으로 교체**

기존 mock import 제거 → `useProjects(activeWorkspaceId)` 사용. `isLoading` / `error` 상태 처리.

- [ ] **Step 3: project-detail.tsx를 useProject 훅으로 교체**

기존 mock 제거 → `useProject(activeWorkspaceId, projectId)` 사용.

- [ ] **Step 4: sidebar.tsx에서 프로젝트 리스트를 실데이터로 교체**

기존 사이드바의 프로젝트 목록이 mock이라면 `useProjects(wid, { status: "active" })`로 교체. 프로젝트 클릭 시 `/projects/{id}` 네비게이션.

- [ ] **Step 5: E2E 확인 — 프로젝트 CRUD가 사이드바에 반영**

```
1. 프로젝트 생성 → 사이드바에 즉시 표시
2. 프로젝트 수정 → 제목 변경 반영
3. 프로젝트 삭제 → 사이드바에서 제거
4. Archive → 목록에서 사라짐
```

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/features/projects/components/ frontend/src/components/layout/sidebar.tsx
git commit -m "feat: Project FE — Mock→Real 전환 (project-list, card, detail, sidebar)"
```

---

## Slice 2: ActionItem BE + FE + 칸반

### Task 6: ActionItem BE — Repository + Service + Router

**Files:**
- Create: `backend/src/actions/schemas.py`
- Create: `backend/src/actions/repository.py`
- Create: `backend/src/actions/service.py`
- Create: `backend/src/actions/dependencies.py`
- Create: `backend/src/actions/exceptions.py`
- Create: `backend/src/actions/router.py`
- Modify: `backend/src/main.py`

- [ ] **Step 1: ActionItem 예외 + 스키마**

```python
# backend/src/actions/exceptions.py
from src.common.exceptions import NotFoundError


class ActionItemNotFoundError(NotFoundError):
    def __init__(self) -> None:
        super().__init__("액션 아이템")
```

```python
# backend/src/actions/schemas.py
from pydantic import BaseModel, Field


class CreateActionItemRequest(BaseModel):
    title: str
    description: str | None = None
    meeting_id: str | None = Field(default=None, alias="meetingId")
    project_id: str | None = Field(default=None, alias="projectId")
    assignee_id: str | None = Field(default=None, alias="assigneeId")
    due_date: str | None = Field(default=None, alias="dueDate")
    priority: str = "medium"

    model_config = {"populate_by_name": True}


class UpdateActionItemRequest(BaseModel):
    title: str | None = None
    description: str | None = None
    project_id: str | None = Field(default=None, alias="projectId")
    assignee_id: str | None = Field(default=None, alias="assigneeId")
    due_date: str | None = Field(default=None, alias="dueDate")
    priority: str | None = None
    status: str | None = None

    model_config = {"populate_by_name": True}
```

- [ ] **Step 2: ActionItem Repository**

```python
# backend/src/actions/repository.py
import uuid

from sqlalchemy import func, select
from sqlalchemy.ext.asyncio import AsyncSession

from src.actions.models import ActionItem


class ActionItemRepository:
    def __init__(self, session: AsyncSession) -> None:
        self.session = session

    async def find_by_id(self, action_id: uuid.UUID) -> ActionItem | None:
        result = await self.session.execute(
            select(ActionItem).where(ActionItem.id == action_id)
        )
        return result.scalar_one_or_none()

    async def find_by_workspace(
        self,
        workspace_id: uuid.UUID,
        status: str | None = None,
        priority: str | None = None,
        project_id: uuid.UUID | None = None,
        offset: int = 0,
        limit: int = 50,
    ) -> list[ActionItem]:
        stmt = select(ActionItem).where(ActionItem.workspace_id == workspace_id)
        if status:
            stmt = stmt.where(ActionItem.status == status)
        if priority:
            stmt = stmt.where(ActionItem.priority == priority)
        if project_id:
            stmt = stmt.where(ActionItem.project_id == project_id)
        stmt = stmt.order_by(ActionItem.created_at.desc()).offset(offset).limit(limit)
        result = await self.session.execute(stmt)
        return list(result.scalars().all())

    async def count_by_workspace(
        self, workspace_id: uuid.UUID, status: str | None = None
    ) -> int:
        stmt = select(func.count()).select_from(ActionItem).where(
            ActionItem.workspace_id == workspace_id
        )
        if status:
            stmt = stmt.where(ActionItem.status == status)
        result = await self.session.execute(stmt)
        return result.scalar_one()

    async def find_by_meeting(self, meeting_id: uuid.UUID) -> list[ActionItem]:
        result = await self.session.execute(
            select(ActionItem)
            .where(ActionItem.meeting_id == meeting_id)
            .order_by(ActionItem.created_at)
        )
        return list(result.scalars().all())

    async def cancel_todo_by_project(self, project_id: uuid.UUID) -> int:
        """프로젝트 archive 시 todo 상태 → cancelled로 변경."""
        result = await self.session.execute(
            select(ActionItem).where(
                ActionItem.project_id == project_id,
                ActionItem.status == "todo",
            )
        )
        items = list(result.scalars().all())
        for item in items:
            item.status = "cancelled"
        await self.session.flush()
        return len(items)

    async def save(self, item: ActionItem) -> ActionItem:
        self.session.add(item)
        await self.session.flush()
        return item

    async def commit(self) -> None:
        await self.session.commit()
```

- [ ] **Step 3: ActionItem Service + Dependencies + Router**

Service는 `MeetingService._to_dict` 패턴을 따르고, Router는 3개 엔드포인트(GET list, POST create, PATCH update). Dependencies는 `get_action_repository → get_action_service` 체인.

`backend/src/main.py`에 `from src.actions.router import router as actions_router` 추가.

- [ ] **Step 4: 서버 기동 테스트**

```bash
cd /Users/woosung/project/agy-project/kairos/backend
uv run uvicorn src.main:app --reload --port 8000
# /api/v1/docs 에서 action-items 엔드포인트 확인
```

- [ ] **Step 5: 커밋**

```bash
git add backend/src/actions/ backend/src/main.py
git commit -m "feat: ActionItem BE — CRUD API 3개 엔드포인트"
```

---

### Task 7: ActionItem FE — API 레이어 + Hook

**Files:**
- Create: `frontend/src/features/actions/api.ts`
- Create: `frontend/src/features/actions/hooks.ts`
- Create: `frontend/src/features/actions/schemas.ts`

- [ ] **Step 1: ActionItem API + Query Key Factory**

```typescript
// frontend/src/features/actions/api.ts
import { apiClient } from "@/lib/api-client";
import type { PaginatedResponse } from "@/types";
import type { ActionItem } from "./types";

export const actionKeys = {
  all: ["actions"] as const,
  list: (wid: string) => [...actionKeys.all, "list", wid] as const,
};

export async function fetchActionItems(
  token: string,
  wid: string,
  params?: { status?: string; priority?: string; projectId?: string; page?: number }
): Promise<PaginatedResponse<ActionItem>> {
  const query = new URLSearchParams();
  if (params?.status) query.set("status", params.status);
  if (params?.priority) query.set("priority", params.priority);
  if (params?.projectId) query.set("projectId", params.projectId);
  if (params?.page) query.set("page", String(params.page));
  const qs = query.toString();
  return apiClient<PaginatedResponse<ActionItem>>(
    `/workspaces/${wid}/action-items${qs ? `?${qs}` : ""}`,
    { token }
  );
}

export async function createActionItem(
  token: string,
  wid: string,
  data: Record<string, unknown>
): Promise<ActionItem> {
  return apiClient<ActionItem>(`/workspaces/${wid}/action-items`, {
    token,
    method: "POST",
    body: JSON.stringify(data),
  });
}

export async function updateActionItem(
  token: string,
  wid: string,
  id: string,
  data: Record<string, unknown>
): Promise<ActionItem> {
  return apiClient<ActionItem>(`/workspaces/${wid}/action-items/${id}`, {
    token,
    method: "PATCH",
    body: JSON.stringify(data),
  });
}
```

- [ ] **Step 2: ActionItem React Query Hooks**

```typescript
// frontend/src/features/actions/hooks.ts
"use client";

import { useAuth } from "@clerk/nextjs";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { actionKeys, fetchActionItems, createActionItem, updateActionItem } from "./api";

export function useActionItems(
  wid: string | undefined,
  params?: { status?: string; priority?: string; projectId?: string }
) {
  const { getToken } = useAuth();
  return useQuery({
    queryKey: [...actionKeys.list(wid ?? ""), params],
    queryFn: async () => {
      const token = await getToken();
      if (!token) throw new Error("인증이 필요합니다");
      return fetchActionItems(token, wid!, params);
    },
    enabled: !!wid,
  });
}

export function useCreateActionItem(wid: string | undefined) {
  const { getToken } = useAuth();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (data: Record<string, unknown>) => {
      const token = await getToken();
      if (!token) throw new Error("인증이 필요합니다");
      return createActionItem(token, wid!, data);
    },
    onSuccess: () => {
      if (wid) queryClient.invalidateQueries({ queryKey: actionKeys.list(wid) });
    },
  });
}

export function useUpdateActionItem(wid: string | undefined) {
  const { getToken } = useAuth();
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async ({ id, data }: { id: string; data: Record<string, unknown> }) => {
      const token = await getToken();
      if (!token) throw new Error("인증이 필요합니다");
      return updateActionItem(token, wid!, id, data);
    },
    onSuccess: () => {
      if (wid) queryClient.invalidateQueries({ queryKey: actionKeys.list(wid) });
    },
  });
}
```

- [ ] **Step 3: 커밋**

```bash
git add frontend/src/features/actions/
git commit -m "feat: ActionItem FE — API 레이어 + React Query 훅"
```

---

### Task 8: 칸반 보드 — dnd-kit 드래그 앤 드롭

**Files:**
- Modify: `frontend/src/features/actions/components/action-kanban.tsx`
- Modify: `frontend/package.json` (dnd-kit 추가)

- [ ] **Step 1: dnd-kit 설치**

```bash
cd /Users/woosung/project/agy-project/kairos/frontend
pnpm add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities
```

- [ ] **Step 2: action-kanban.tsx 재구현**

기존 파일을 읽고 전체 교체. 핵심 구조:

```typescript
// frontend/src/features/actions/components/action-kanban.tsx
"use client";

import { useState } from "react";
import {
  DndContext,
  DragOverlay,
  closestCorners,
  type DragEndEvent,
  type DragStartEvent,
} from "@dnd-kit/core";
import { useActionItems, useUpdateActionItem } from "../hooks";
import { useWorkspaceStore } from "@/features/workspaces/store";
import type { ActionItem } from "../types";

const COLUMNS = [
  { id: "todo", label: "Todo" },
  { id: "in_progress", label: "In Progress" },
  { id: "done", label: "Done" },
] as const;

type ColumnId = (typeof COLUMNS)[number]["id"];

export function ActionKanban() {
  const activeWorkspaceId = useWorkspaceStore((s) => s.activeWorkspaceId);
  const { data } = useActionItems(activeWorkspaceId ?? undefined);
  const updateAction = useUpdateActionItem(activeWorkspaceId ?? undefined);
  const [activeItem, setActiveItem] = useState<ActionItem | null>(null);

  const items = data?.items ?? [];
  const grouped = COLUMNS.reduce(
    (acc, col) => {
      acc[col.id] = items.filter((item) => item.status === col.id);
      return acc;
    },
    {} as Record<ColumnId, ActionItem[]>
  );

  function handleDragStart(event: DragStartEvent) {
    const item = items.find((i) => i.id === event.active.id);
    setActiveItem(item ?? null);
  }

  function handleDragEnd(event: DragEndEvent) {
    setActiveItem(null);
    const { active, over } = event;
    if (!over) return;

    const newStatus = over.id as string;
    const item = items.find((i) => i.id === active.id);
    if (!item || item.status === newStatus) return;

    // 낙관적 업데이트: mutate 호출
    updateAction.mutate({ id: item.id, data: { status: newStatus } });
  }

  return (
    <DndContext
      collisionDetection={closestCorners}
      onDragStart={handleDragStart}
      onDragEnd={handleDragEnd}
    >
      <div className="flex gap-4 p-4 overflow-x-auto">
        {COLUMNS.map((col) => (
          <KanbanColumn key={col.id} id={col.id} label={col.label} items={grouped[col.id]} />
        ))}
      </div>
      <DragOverlay>
        {activeItem ? <KanbanCard item={activeItem} isDragging /> : null}
      </DragOverlay>
    </DndContext>
  );
}
```

`KanbanColumn` (droppable)과 `KanbanCard` (draggable)을 같은 파일 내 정의.
- `KanbanColumn`: `useDroppable` 사용, column id = status
- `KanbanCard`: `useDraggable` 사용, id = actionItem.id
- 우선순위 색상: `high=#F87171`, `medium=#FBBF24`, `low=#60A5FA`

- [ ] **Step 3: action-list.tsx hook 연결**

기존 mock 데이터 → `useActionItems(wid)` 훅으로 교체.

- [ ] **Step 4: E2E 확인 — 칸반 드래그 상태 변경**

```
1. 액션 아이템 수동 생성 (/api/v1/docs에서)
2. 칸반에서 카드 표시 확인
3. Todo → In Progress 드래그 → 상태 변경 API 호출 확인
4. 새로고침 후 상태 유지 확인
```

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/features/actions/ frontend/package.json frontend/pnpm-lock.yaml
git commit -m "feat: ActionItem FE — dnd-kit 칸반 보드 + Mock→Real 전환"
```

---

## Slice 3: Inbox + AI 파이프라인 확장

### Task 9: AI 파이프라인 확장 — 프롬프트 + 서비스

**Files:**
- Modify: `backend/src/common/prompts.py`
- Modify: `backend/src/services/ai_processing.py`

- [ ] **Step 1: MEETING_ACTIONS_AND_LINKING 프롬프트 추가**

`backend/src/common/prompts.py`에 추가:

```python
MEETING_ACTIONS_AND_LINKING_PROMPT = """당신은 회의 트랜스크립트를 분석하여 액션 아이템을 추출하고, 
관련 프로젝트를 추천하는 AI 비서입니다.

## 트랜스크립트
{transcript}

## AI 요약
{summary}

## 기존 프로젝트 목록
{projects_context}

## 지시사항
1. 트랜스크립트에서 구체적인 액션 아이템(할 일, 결정된 작업)을 추출하세요.
2. 기존 프로젝트 목록과 비교하여 가장 관련 있는 프로젝트를 추천하세요.
3. 관련 프로젝트가 없으면 새 프로젝트 이름을 제안하세요.
4. 회의 내용에 맞는 태그를 추천하세요.

반드시 아래 JSON 형식으로만 응답하세요:
{
  "actionItems": [
    {
      "title": "구체적인 액션 아이템 제목",
      "description": "상세 설명 (선택)",
      "priority": "high 또는 medium 또는 low",
      "dueDate": "YYYY-MM-DD 또는 null"
    }
  ],
  "suggestedProject": {
    "existingProjectId": "기존 프로젝트 UUID 또는 null",
    "newProjectTitle": "새 프로젝트 이름 또는 null",
    "confidence": 0.0에서 1.0 사이의 확신도
  },
  "suggestedTags": ["태그1", "태그2"]
}"""
```

- [ ] **Step 2: ai_processing.py에 extract_actions_and_link 메서드 추가**

```python
# backend/src/services/ai_processing.py 에 추가

async def extract_actions_and_link(
    self,
    transcript: str,
    summary: str,
    existing_projects: list[dict],
) -> dict:
    """트랜스크립트 → 액션 아이템 + 프로젝트 연결 추천."""
    projects_context = "\n".join(
        f"- {p['id']}: {p['title']} ({p['status']})"
        for p in existing_projects
    ) or "기존 프로젝트 없음"

    prompt = MEETING_ACTIONS_AND_LINKING_PROMPT.format(
        transcript=transcript,
        summary=summary,
        projects_context=projects_context,
    )

    response = await self.client.aio.models.generate_content(
        model=GEMINI_MODEL,
        contents=prompt,
    )
    return parse_json_response(response.text)
```

- [ ] **Step 3: 커밋**

```bash
git add backend/src/common/prompts.py backend/src/services/ai_processing.py
git commit -m "feat: AI 파이프라인 — 액션 추출 + 프로젝트 연결 프롬프트 추가"
```

---

### Task 10: Inbox BE — Repository + Service + Router

**Files:**
- Create: `backend/src/inbox/schemas.py`
- Create: `backend/src/inbox/repository.py`
- Create: `backend/src/inbox/service.py`
- Create: `backend/src/inbox/dependencies.py`
- Create: `backend/src/inbox/exceptions.py`
- Create: `backend/src/inbox/router.py`
- Modify: `backend/src/main.py`

- [ ] **Step 1: Inbox 예외 + 스키마**

```python
# backend/src/inbox/exceptions.py
from src.common.exceptions import NotFoundError

class InboxItemNotFoundError(NotFoundError):
    def __init__(self) -> None:
        super().__init__("Inbox 아이템")
```

```python
# backend/src/inbox/schemas.py
from pydantic import BaseModel, Field

class ClassifyInboxRequest(BaseModel):
    project_ids: list[str] = Field(alias="projectIds")
    model_config = {"populate_by_name": True}
```

- [ ] **Step 2: Inbox Repository + Service**

Repository: `find_by_workspace`, `find_by_id`, `save`, `commit`
Service: `list_inbox`, `classify` (MeetingProjectLink 생성 + isProcessed=True), `dismiss` (isProcessed=True)

classify 로직에서 `ProjectRepository.add_meeting_link`를 호출해야 하므로, Dependencies에서 동일 session으로 `InboxRepository + ProjectRepository`를 주입.

- [ ] **Step 3: Inbox Router (3개 엔드포인트) + main.py 등록**

```python
# GET  /api/v1/workspaces/{wid}/inbox
# POST /api/v1/workspaces/{wid}/inbox/{id}/classify
# POST /api/v1/workspaces/{wid}/inbox/{id}/dismiss
```

- [ ] **Step 4: 커밋**

```bash
git add backend/src/inbox/ backend/src/main.py
git commit -m "feat: Inbox BE — CRUD + classify/dismiss API"
```

---

### Task 11: 오케스트레이터 확장 — 파이프라인에 액션+Inbox 통합

**Files:**
- Modify: `backend/src/meetings/pipeline_service.py`
- Modify: `backend/src/meetings/dependencies.py`
- Modify: `backend/src/meetings/models.py`

- [ ] **Step 1: Meeting 상태에서 "embedding" 제거**

`backend/src/meetings/models.py` 및 관련 코드에서 `embedding` 상태 참조 제거. 파이프라인 흐름: `uploading → transcribing → analyzing → completed | failed`

- [ ] **Step 2: pipeline_service.py 확장**

기존 `process_meeting`에 액션 추출 + Inbox 적재 + 자동 확정 단계 추가:

```python
# 기존 흐름 이후에 추가:
# [3] 액션 추출 + 프로젝트 연결
existing_projects = await self.project_repo.find_by_workspace(meeting.workspace_id)
project_list = [{"id": str(p.id), "title": p.title, "status": p.status} for p in existing_projects]

actions_data = await self.ai_service.extract_actions_and_link(
    transcript_text, summary_data.get("summary", ""), project_list
)

# [4] ActionItem 저장
for ai_action in actions_data.get("actionItems", []):
    action_item = ActionItem(
        workspace_id=meeting.workspace_id,
        meeting_id=meeting.id,
        title=ai_action["title"],
        description=ai_action.get("description"),
        priority=ai_action.get("priority", "medium"),
    )
    await self.action_repo.save(action_item)

# [5] InboxItem 생성
suggested = actions_data.get("suggestedProject", {})
confidence = suggested.get("confidence", 0.0)
existing_project_id = suggested.get("existingProjectId")

inbox_item = InboxItem(
    workspace_id=meeting.workspace_id,
    title=f"{meeting.title} 요약",
    summary=summary_data.get("summary", ""),
    source_type="meeting",
    source_id=meeting.id,
    ai_suggested_project_id=uuid.UUID(existing_project_id) if existing_project_id else None,
    ai_suggested_project_title=suggested.get("newProjectTitle"),
    ai_suggested_tags=actions_data.get("suggestedTags", []),
    ai_confidence=confidence,
    is_processed=confidence >= 0.8,  # 자동 확정
)
await self.inbox_repo.save(inbox_item)

# [6] 자동 확정 시 MeetingProjectLink 생성
if confidence >= 0.8 and existing_project_id:
    await self.project_repo.add_meeting_link(
        meeting.id, uuid.UUID(existing_project_id)
    )

# 업데이트: actionItemCount
meeting.action_item_count = len(actions_data.get("actionItems", []))
```

- [ ] **Step 3: dependencies.py에 신규 repo 주입**

`get_pipeline_service`에 `ProjectRepository`, `ActionItemRepository`, `InboxRepository`를 동일 session으로 주입.

- [ ] **Step 4: E2E 확인**

```
1. 프로젝트 1개 생성
2. 회의 업로드
3. 처리 완료 후:
   - 액션 아이템이 생성되었는지 확인 (GET /action-items)
   - Inbox에 아이템이 적재되었는지 확인 (GET /inbox)
   - confidence ≥ 0.8이면 MeetingProjectLink 자동 생성 확인
```

- [ ] **Step 5: 커밋**

```bash
git add backend/src/meetings/ backend/src/services/
git commit -m "feat: 오케스트레이터 확장 — 액션 추출 + Inbox 적재 + 자동 확정"
```

---

### Task 12: Inbox FE — API 레이어 + 컴포넌트 연결

**Files:**
- Create: `frontend/src/features/inbox/api.ts`
- Create: `frontend/src/features/inbox/hooks.ts`
- Modify: `frontend/src/features/inbox/types.ts`
- Modify: `frontend/src/features/inbox/components/inbox-list.tsx`
- Modify: `frontend/src/features/inbox/components/inbox-item-card.tsx`

- [ ] **Step 1: Inbox 타입 수정 (PARA → Project)**

`frontend/src/features/inbox/types.ts`에서 `aiSuggestedParaType/Id/Title` → `aiSuggestedProjectId/Title/Tags` 변경.

- [ ] **Step 2: Inbox API + Query Key Factory + Hooks**

패턴은 Task 4, 7과 동일. `inboxKeys.list(wid)`, `fetchInbox`, `classifyInbox`, `dismissInbox` 함수 + 대응 hooks.

- [ ] **Step 3: inbox-item-card.tsx 프로젝트 연결 UI 구현**

```
AI 추천: "CMS 고도화" (87%) [확정] [변경] [무시]
- [확정] → useClassifyInbox({ id, projectIds: [suggestedId] })
- [변경] → ProjectCombobox 열림
- [무시] → useDismissInbox(id)
```

- [ ] **Step 4: inbox-list.tsx hook 연결**

mock → `useInbox(wid)` 훅 교체.

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/features/inbox/
git commit -m "feat: Inbox FE — API 연결 + 프로젝트 분류 UI"
```

---

### Task 13: ProjectCombobox + 회의 상세 프로젝트 연결 + 토스트

**Files:**
- Create: `frontend/src/features/projects/components/project-combobox.tsx`
- Modify: `frontend/src/features/meetings/components/meeting-detail.tsx`
- Modify: `frontend/src/features/meetings/types.ts`

- [ ] **Step 1: ProjectCombobox 공통 컴포넌트**

프로젝트 검색 + 선택 + "새 프로젝트 만들기" 옵션. shadcn/ui `Popover` + `Command` 사용.

```typescript
// frontend/src/features/projects/components/project-combobox.tsx
"use client";

import { useState } from "react";
import { useProjects, useCreateProject } from "../hooks";
import { useWorkspaceStore } from "@/features/workspaces/store";

interface ProjectComboboxProps {
  onSelect: (projectId: string) => void;
}

export function ProjectCombobox({ onSelect }: ProjectComboboxProps) {
  const [search, setSearch] = useState("");
  const activeWorkspaceId = useWorkspaceStore((s) => s.activeWorkspaceId);
  const { data } = useProjects(activeWorkspaceId ?? undefined, { status: "active" });
  const createProject = useCreateProject(activeWorkspaceId ?? undefined);

  const filtered = (data?.items ?? []).filter((p) =>
    p.title.toLowerCase().includes(search.toLowerCase())
  );

  async function handleCreate() {
    if (!search.trim()) return;
    const result = await createProject.mutateAsync({ title: search.trim() });
    onSelect(result.id);
  }

  return (
    // Popover + Command UI (shadcn/ui 패턴)
    // 검색 입력, 필터된 프로젝트 리스트, "새 프로젝트 만들기" 옵션
  );
}
```

- [ ] **Step 2: meeting-detail.tsx에 프로젝트 연결 섹션 추가**

기존 `meeting-detail.tsx`의 메타데이터 영역 아래에 "연결된 프로젝트" 섹션 추가:
- 프로젝트 뱃지 나열 (`meeting.projects`)
- [+ 추가] 버튼 → `ProjectCombobox` 열림
- 뱃지 X 버튼 → `useRemoveMeetingProject` 호출

- [ ] **Step 3: MeetingDetail 타입에서 "embedding" 제거**

`frontend/src/features/meetings/types.ts`에서 `MeetingStatus` 타입에서 `"embedding"` 제거.

- [ ] **Step 4: 토스트 설치 확인 + useInboxAutoClassifyToast 구현**

```bash
# sonner 설치 확인 (shadcn/ui 토스트)
cd /Users/woosung/project/agy-project/kairos/frontend
pnpm dlx shadcn@latest add sonner  # 이미 있으면 스킵
```

`features/inbox/hooks.ts`에 `useInboxAutoClassifyToast` 추가:
- `useMeetingStatus`가 `completed` 반환 시
- `useInbox(wid, { isProcessed: true })`에서 최신 자동 확정 항목 확인
- `toast({ title: "프로젝트에 연결됨", action: { label: "되돌리기", onClick: ... } })`

- [ ] **Step 5: 업로드 진행률 UI 개선**

`frontend/src/features/meetings/components/meeting-detail.tsx`의 처리 중 표시를 단계별로 개선:
- `uploading` → "업로드 중..." + Progress bar (기존 `usePresignedUpload` progress 연결)
- `transcribing` → "음성 인식 중..." + 스피너
- `analyzing` → "AI 분석 중..." + 스피너
- `completed` → "회의 보기" 링크 + 토스트

- [ ] **Step 6: E2E 확인 — 전체 체인**

```
1. 프로젝트 "CMS 고도화" 생성
2. 회의 업로드
3. 처리 완료 → 회의 상세에서 연결된 프로젝트 확인
4. Inbox에서 아이템 확인 → [확정] 또는 [변경]
5. 토스트 알림 확인 (자동 확정 시)
6. 회의 상세에서 [+ 추가]로 다른 프로젝트 연결
7. 뱃지 X로 프로젝트 연결 해제
```

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/features/projects/components/project-combobox.tsx \
        frontend/src/features/meetings/ \
        frontend/src/features/inbox/
git commit -m "feat: ProjectCombobox + 회의-프로젝트 연결 + 자동확정 토스트 + 업로드 진행률"
```

---

## Slice 4: 인프라 (병렬 — 별도 worktree)

> 이 Slice는 Slice 1~3과 독립적이므로 별도 worktree/브랜치에서 병렬 작업 가능.

### Task 14: ffmpeg 변환 — BE

**Files:**
- Modify: `backend/src/services/transcription.py`
- Modify: `backend/Dockerfile` (ffmpeg 추가)

- [ ] **Step 1: transcription.py에 convert_to_wav 추가**

```python
# backend/src/services/transcription.py 에 추가
import asyncio
import os
import tempfile

async def convert_to_wav(input_path: str) -> str:
    """ffmpeg로 입력 파일을 wav 16kHz mono로 변환. 이미 wav면 스킵."""
    # ffprobe로 포맷 감지
    probe_cmd = f"ffprobe -v quiet -print_format json -show_format {input_path}"
    proc = await asyncio.create_subprocess_shell(
        probe_cmd,
        stdout=asyncio.subprocess.PIPE,
        stderr=asyncio.subprocess.PIPE,
    )
    stdout, _ = await proc.communicate()
    
    import json
    format_info = json.loads(stdout)
    format_name = format_info.get("format", {}).get("format_name", "")
    
    # 이미 wav면 스킵
    if format_name == "wav":
        return input_path
    
    # wav로 변환
    output_path = tempfile.mktemp(suffix=".wav")
    convert_cmd = (
        f"ffmpeg -y -i {input_path} "
        f"-ar 16000 -ac 1 -c:a pcm_s16le {output_path}"
    )
    proc = await asyncio.create_subprocess_shell(
        convert_cmd,
        stdout=asyncio.subprocess.PIPE,
        stderr=asyncio.subprocess.PIPE,
    )
    _, stderr = await proc.communicate()
    
    if proc.returncode != 0:
        raise RuntimeError(f"ffmpeg 변환 실패: {stderr.decode()}")
    
    return output_path
```

- [ ] **Step 2: pipeline에서 convert_to_wav 호출**

`pipeline_service.py`의 STT 단계 전에 ffmpeg 변환 호출:

```python
# transcribing 단계에서
audio_path = await self.transcription_service.download_to_temp(audio_url)
wav_path = await convert_to_wav(audio_path)
segments, duration = await self.transcription_service.transcribe_file(wav_path)
# 임시 파일 정리
if wav_path != audio_path:
    os.unlink(wav_path)
os.unlink(audio_path)
```

- [ ] **Step 3: Dockerfile에 ffmpeg 추가**

```dockerfile
# backend/Dockerfile
RUN apt-get update && apt-get install -y ffmpeg && rm -rf /var/lib/apt/lists/*
```

- [ ] **Step 4: 로컬 테스트 — 카카오톡 m4a 업로드**

```
1. 카카오톡에서 녹음한 .m4a 파일 준비
2. 업로드 → ffmpeg 변환 → Whisper STT → 요약 성공 확인
3. 일반 mp3 파일도 기존처럼 동작하는지 확인 (회귀 테스트)
```

- [ ] **Step 5: 커밋**

```bash
git add backend/src/services/transcription.py backend/Dockerfile
git commit -m "feat: ffmpeg 오디오 변환 — 카카오톡 m4a(3GP) 호환"
```

---

### Task 15: 반응형 웹 — CSS 변수 + useMediaQuery + Zustand

**Files:**
- Create: `frontend/src/hooks/use-media-query.ts`
- Modify: `frontend/src/store/ui.ts`
- Modify: `frontend/src/app/globals.css`

- [ ] **Step 1: globals.css에 반응형 CSS 변수 추가**

```css
/* frontend/src/app/globals.css :root에 추가 */
--sidebar-width: 220px;
--sidebar-collapsed-width: 48px;
--rag-panel-width: 320px;
```

- [ ] **Step 2: useMediaQuery 훅 생성**

```typescript
// frontend/src/hooks/use-media-query.ts
"use client";

import { useEffect, useState } from "react";

export function useMediaQuery(query: string): boolean {
  const [matches, setMatches] = useState(false);

  useEffect(() => {
    const media = window.matchMedia(query);
    setMatches(media.matches);
    const handler = (e: MediaQueryListEvent) => setMatches(e.matches);
    media.addEventListener("change", handler);
    return () => media.removeEventListener("change", handler);
  }, [query]);

  return matches;
}

export function useBreakpoint() {
  const isMobile = !useMediaQuery("(min-width: 768px)");
  const isCompact = useMediaQuery("(min-width: 768px)") && !useMediaQuery("(min-width: 1280px)");
  const isDesktop = useMediaQuery("(min-width: 1280px)");
  return { isMobile, isCompact, isDesktop };
}
```

- [ ] **Step 3: Zustand ui.ts 확장**

```typescript
// frontend/src/store/ui.ts — 추가 필드
interface UIState {
  sidebarOpen: boolean;
  sidebarCollapsed: boolean;  // 신규
  ragPanelOpen: boolean;
  cmdKOpen: boolean;
  isMobile: boolean;  // 신규
  toggleSidebar: () => void;
  toggleRagPanel: () => void;
  toggleCmdK: () => void;
  setSidebarCollapsed: (collapsed: boolean) => void;  // 신규
  setIsMobile: (mobile: boolean) => void;  // 신규
}
```

- [ ] **Step 4: 커밋**

```bash
git add frontend/src/hooks/use-media-query.ts frontend/src/store/ui.ts frontend/src/app/globals.css
git commit -m "feat: 반응형 인프라 — useMediaQuery + Zustand 확장 + CSS 변수"
```

---

### Task 16: 반응형 웹 — 레이아웃 컴포넌트 적용

**Files:**
- Modify: `frontend/src/components/layout/panel-layout.tsx`
- Modify: `frontend/src/components/layout/sidebar.tsx`
- Create: `frontend/src/components/layout/bottom-nav.tsx`
- Modify: `frontend/src/app/(app)/layout.tsx`

- [ ] **Step 1: panel-layout.tsx 반응형 적용**

```typescript
// 핵심 변경: useBreakpoint()로 분기
const { isMobile, isCompact, isDesktop } = useBreakpoint();

// Desktop: 3-Panel 풀
// Compact: 사이드바 48px + 메인 + RAG 접힘
// Mobile: 메인만 + 하단 nav
```

- `isDesktop`: 사이드바 220px + RAG 320px 상시
- `isCompact`: 사이드바 48px (아이콘 모드) + RAG 버튼 토글
- `isMobile`: 사이드바 숨김, RAG 숨김, 하단 nav 표시

- [ ] **Step 2: sidebar.tsx 아이콘 모드**

`sidebarCollapsed` 상태에 따라:
- `false`: 220px, 텍스트+아이콘 (기존)
- `true`: 48px, 아이콘만. hover 시 오버레이로 확장

- [ ] **Step 3: bottom-nav.tsx 생성**

```typescript
// frontend/src/components/layout/bottom-nav.tsx
"use client";

import Link from "next/link";
import { Home, FolderOpen, Plus, Inbox, Search } from "lucide-react";

const NAV_ITEMS = [
  { href: "/", icon: Home, label: "홈" },
  { href: "/projects", icon: FolderOpen, label: "프로젝트" },
  { href: "/new", icon: Plus, label: "업로드", isAccent: true },
  { href: "/inbox", icon: Inbox, label: "Inbox" },
  { href: "/search", icon: Search, label: "검색" },
];

export function BottomNav() {
  return (
    <nav className="fixed bottom-0 left-0 right-0 md:hidden flex items-center justify-around h-14"
         style={{ background: "var(--surface)", borderTop: "1px solid var(--border)" }}>
      {NAV_ITEMS.map((item) => (
        <Link key={item.href} href={item.href} className="flex flex-col items-center gap-0.5 p-2">
          <item.icon
            size={20}
            style={{ color: item.isAccent ? "var(--accent)" : "var(--text-muted)" }}
          />
          <span className="text-[10px]" style={{ color: "var(--text-muted)" }}>
            {item.label}
          </span>
        </Link>
      ))}
    </nav>
  );
}
```

- [ ] **Step 4: layout.tsx에 BottomNav 추가**

`app/(app)/layout.tsx`에서 `<BottomNav />` 추가. `md:hidden`으로 768px 이상 숨김. 모바일에서 하단 nav 영역만큼 `pb-14` padding 추가.

- [ ] **Step 5: 반응형 테스트**

```
1. 브라우저 1280px → 3-Panel 풀 레이아웃
2. 브라우저 900px → 사이드바 아이콘 모드 + RAG 접힘
3. 브라우저 600px → 단일 패널 + 하단 네비게이션
4. 사이드바 hover 오버레이 동작 확인
5. RAG 토글 버튼 동작 확인 (Compact)
```

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/components/layout/ frontend/src/app/\(app\)/layout.tsx
git commit -m "feat: 반응형 3단계 — Desktop/Compact/Mobile 레이아웃"
```

---

### Task 17: 문서 업데이트

**Files:**
- Modify: `DESIGN.md`
- Modify: `.ai/rules/frontend.md`
- Modify: `docs/api/endpoints.md`
- Modify: `docs/architecture/erd.md`

- [ ] **Step 1: DESIGN.md Layout 섹션에 반응형 추가**

Layout 섹션 끝에 반응형 breakpoint 표 추가:

```markdown
### Responsive Breakpoints
| 이름 | 범위 | 레이아웃 |
|------|------|----------|
| Desktop | ≥1280px | 3-Panel 풀 |
| Compact | 768~1279px | 사이드바 48px + RAG 접힘 |
| Mobile | <768px | 단일 패널 + 하단 nav |
```

- [ ] **Step 2: frontend.md에 반응형 규칙 추가**

```markdown
### 반응형 규칙
- Desktop-first 접근 (기존 코드 기준)
- Breakpoint: `xl:` (≥1280px 기본), `md:` (768px compact), 기본 (mobile)
- 고정 px 대신 CSS 변수 사용 (`--sidebar-width`, `--rag-panel-width`)
- Mobile: `md:hidden`으로 데스크톱 전용 요소 숨김
```

- [ ] **Step 3: endpoints.md PARA 제거 반영**

Sprint 2 섹션에서:
- `category` 관련 필드/파라미터 삭제
- `aiSuggestedPara*` → `aiSuggestedProject*` 변경
- MeetingProject 연결 API 2개 추가

- [ ] **Step 4: erd.md에서 category 제거**

Project 엔티티에서 `category` 필드 삭제.

- [ ] **Step 5: 커밋**

```bash
git add DESIGN.md .ai/rules/frontend.md docs/api/endpoints.md docs/architecture/erd.md
git commit -m "docs: Sprint 2 문서 업데이트 — 반응형 규칙, PARA 제거, API 변경"
```

---

## 완료 기준 체크리스트

- [ ] Slice 1: 프로젝트 CRUD → 사이드바 실시간 반영
- [ ] Slice 2: 칸반 드래그 → 상태 변경 + API 동기화
- [ ] Slice 3: 회의 업로드 → AI 요약 + 액션 추출 → Inbox → 프로젝트 연결 전체 E2E
- [ ] Slice 4-a: 카카오톡 m4a 업로드 → ffmpeg 변환 → STT 성공
- [ ] Slice 4-b: 768px 이하 모바일 레이아웃 + 하단 nav
- [ ] 문서: DESIGN.md, frontend.md, endpoints.md, erd.md 업데이트
