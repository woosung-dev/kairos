# BL-003 + BL-004 구현 플랜

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** RAG N+1 쿼리를 배치로 줄이고(BL-003), LLM 응답 스키마 위반 시 즉시 에러를 내도록 Pydantic 검증을 추가한다(BL-004).

**Architecture:** BL-004를 먼저 처리(공통 모델 정의 → ai_processing 검증 추가), 이후 BL-003(Repository 배치 메서드 → service 리팩터). 두 변경 모두 기존 caller 인터페이스 변경 없음.

**Tech Stack:** FastAPI, SQLModel, Pydantic V2, pytest-asyncio, uv

---

## 파일 구조

| 파일 | 변경 종류 | 내용 |
|------|----------|------|
| `backend/src/common/prompts.py` | 수정 | Pydantic 모델 2개 추가 (BL-004) |
| `backend/src/services/ai_processing.py` | 수정 | import 1줄 + 검증 2줄 추가 (BL-004) |
| `backend/src/embeddings/repository.py` | 수정 | `find_chunks_by_ids` 메서드 추가 (BL-003) |
| `backend/src/rag/service.py` | 수정 | `_enrich_context` 배치 호출로 변경 (BL-003) |
| `backend/tests/services/test_ai_processing.py` | 수정 | 스키마 검증 테스트 4개 추가 |
| `backend/tests/rag/test_rag_service.py` | 수정 | `_enrich_context` 배치 테스트 3개 추가 |

---

## Task 1: BL-004 — Pydantic 스키마 모델 정의

**Files:**
- Modify: `backend/src/common/prompts.py`

- [ ] **Step 1: `common/prompts.py` 하단에 Pydantic 모델 추가**

`backend/src/common/prompts.py` 파일 맨 마지막에 다음을 추가한다.

```python
# ── LLM 응답 계약 스키마 ──
# 프롬프트 출력 스키마 텍스트와 동기화 유지. 키 변경 시 프롬프트 텍스트와 함께 수정할 것.
from pydantic import BaseModel


class MeetingSummaryResult(BaseModel):
    summary: str
    key_decisions: list[str] = []
    risks_and_issues: list[str] = []
    participants: list[str] = []
    topics: list[str] = []
    next_meeting_agenda: list[str] = []


class MeetingActionsResult(BaseModel):
    actionItems: list[dict] = []
    suggestedProject: dict = {}
    suggestedTags: list[str] = []
```

- [ ] **Step 2: 즉시 임포트 확인**

```bash
cd backend && uv run python -c "from src.common.prompts import MeetingSummaryResult, MeetingActionsResult; print('OK')"
```

Expected output:
```
OK
```

---

## Task 2: BL-004 — ai_processing.py 검증 추가 (TDD)

**Files:**
- Modify: `backend/tests/services/test_ai_processing.py`
- Modify: `backend/src/services/ai_processing.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`backend/tests/services/test_ai_processing.py` 파일 맨 아래에 추가한다. (기존 3개 테스트 유지)

```python
# ── BL-004: 스키마 검증 테스트 ──
from pydantic import ValidationError
from src.common.prompts import MeetingSummaryResult, MeetingActionsResult


def test_meeting_summary_result_valid():
    """유효한 요약 응답 → 검증 통과."""
    data = {
        "summary": "CMS 고도화 킥오프.",
        "key_decisions": ["3월 내 완료"],
        "risks_and_issues": [],
        "participants": ["김철수"],
        "topics": ["CMS"],
        "next_meeting_agenda": [],
    }
    result = MeetingSummaryResult.model_validate(data)
    assert result.summary == "CMS 고도화 킥오프."
    assert result.key_decisions == ["3월 내 완료"]


def test_meeting_summary_result_missing_summary():
    """summary 필드 누락 → ValidationError."""
    with pytest.raises(ValidationError):
        MeetingSummaryResult.model_validate({})


def test_meeting_summary_result_defaults():
    """선택 필드 누락 시 빈 리스트 기본값."""
    result = MeetingSummaryResult.model_validate({"summary": "요약"})
    assert result.key_decisions == []
    assert result.topics == []


def test_meeting_actions_result_defaults():
    """actionItems 없어도 기본값으로 처리."""
    result = MeetingActionsResult.model_validate({})
    assert result.actionItems == []
    assert result.suggestedTags == []
```

- [ ] **Step 2: 테스트 실행 — PASS 확인** (모델 자체 테스트라 바로 통과)

```bash
cd backend && uv run pytest tests/services/test_ai_processing.py::test_meeting_summary_result_valid tests/services/test_ai_processing.py::test_meeting_summary_result_missing_summary tests/services/test_ai_processing.py::test_meeting_summary_result_defaults tests/services/test_ai_processing.py::test_meeting_actions_result_defaults -v
```

Expected: 4 passed

- [ ] **Step 3: `ai_processing.py` import 추가**

`backend/src/services/ai_processing.py` 상단 import 블록을 다음으로 변경한다.

```python
from src.common.prompts import (
    MEETING_ACTIONS_AND_LINKING_PROMPT,
    MEETING_SUMMARY_SYSTEM_PROMPT,
    RAG_SYSTEM_PROMPT,
    MeetingActionsResult,
    MeetingSummaryResult,
    parse_json_response,
)
```

- [ ] **Step 4: `summarize()` 검증 추가**

`backend/src/services/ai_processing.py`의 `summarize` 메서드를 다음으로 교체한다.

```python
async def summarize(self, transcript: str) -> dict:
    """트랜스크립트 → 구조화된 요약 dict.

    반환 형식:
    {
        "summary": str,
        "key_decisions": list[str],
        "risks_and_issues": list[str],
        "participants": list[str],
        "topics": list[str],
        "next_meeting_agenda": list[str],
    }
    """
    response = await self.client.aio.models.generate_content(
        model=GEMINI_MODEL,
        contents=f"{MEETING_SUMMARY_SYSTEM_PROMPT}\n\n{transcript}",
    )
    raw = parse_json_response(response.text)
    MeetingSummaryResult.model_validate(raw)
    return raw
```

- [ ] **Step 5: `extract_actions_and_link()` 검증 추가**

`backend/src/services/ai_processing.py`의 `extract_actions_and_link` 메서드를 다음으로 교체한다.

```python
async def extract_actions_and_link(
    self,
    transcript: str,
    summary: str,
    existing_projects: list[dict],
) -> dict:
    """트랜스크립트 → 액션 아이템 + 프로젝트 연결 추천.

    반환 형식:
    {
        "actionItems": [{"title": str, "description": str|None, "priority": str, "dueDate": str|None}],
        "suggestedProject": {"existingProjectId": str|None, "newProjectTitle": str|None, "confidence": float},
        "suggestedTags": list[str],
    }
    """
    projects_context = "\n".join(
        f"- {p['id']}: {p['title']} ({p['status']})"
        for p in existing_projects
    ) or "기존 프로젝트 없음"

    prompt = MEETING_ACTIONS_AND_LINKING_PROMPT.format(
        transcript=transcript,
        summary=summary,
        projects_context=projects_context,
    )

    response = await self.client.aio.models.generate_content(
        model=GEMINI_MODEL,
        contents=prompt,
    )
    raw = parse_json_response(response.text)
    MeetingActionsResult.model_validate(raw)
    return raw
```

- [ ] **Step 6: 기존 테스트 포함 전체 실행 — PASS 확인**

```bash
cd backend && uv run pytest tests/services/test_ai_processing.py -v
```

Expected: 7 passed (기존 3 + 신규 4)

- [ ] **Step 7: 커밋**

```bash
cd backend && git add src/common/prompts.py src/services/ai_processing.py tests/services/test_ai_processing.py
git commit -m "feat(ai_processing): BL-004 LLM 응답 계약 Pydantic 검증 추가"
```

---

## Task 3: BL-003 — EmbeddingRepository 배치 메서드 추가

**Files:**
- Modify: `backend/src/embeddings/repository.py`

- [ ] **Step 1: `find_chunks_by_ids` 메서드 추가**

`backend/src/embeddings/repository.py`에서 `find_chunk_by_id` 메서드 바로 아래에 다음을 추가한다.

```python
async def find_chunks_by_ids(
    self, ids: list[uuid.UUID]
) -> dict[uuid.UUID, EmbeddingChunk]:
    """여러 청크를 한 번의 쿼리로 조회한다."""
    if not ids:
        return {}
    result = await self.session.execute(
        select(EmbeddingChunk).where(EmbeddingChunk.id.in_(ids))
    )
    return {c.id: c for c in result.scalars().all()}
```

- [ ] **Step 2: 임포트 확인**

```bash
cd backend && uv run python -c "from src.embeddings.repository import EmbeddingRepository; print('OK')"
```

Expected output:
```
OK
```

---

## Task 4: BL-003 — `_enrich_context` 배치 리팩터 (TDD)

**Files:**
- Modify: `backend/tests/rag/test_rag_service.py`
- Modify: `backend/src/rag/service.py`

- [ ] **Step 1: 실패하는 테스트 작성**

`backend/tests/rag/test_rag_service.py` 파일 맨 아래에 추가한다.

```python
# ── BL-003: _enrich_context 배치 쿼리 테스트 ──
import uuid
from unittest.mock import AsyncMock, MagicMock


@pytest.mark.asyncio
async def test_enrich_context_uses_batch_query():
    """parent_chunk_id가 있는 결과 → find_chunks_by_ids 1회 호출."""
    parent_id = uuid.uuid4()
    mock_chunk = MagicMock()
    mock_chunk.chunk_text = "부모 청크 텍스트"

    mock_repo = AsyncMock()
    mock_repo.find_chunks_by_ids.return_value = {parent_id: mock_chunk}

    service = RagService(
        embedding_repo=mock_repo,
        embedding_service=AsyncMock(),
        ai_service=AsyncMock(),
    )

    results = [
        {"id": str(uuid.uuid4()), "chunk_text": "자식1", "parent_chunk_id": str(parent_id)},
        {"id": str(uuid.uuid4()), "chunk_text": "자식2", "parent_chunk_id": str(parent_id)},
    ]
    enriched = await service._enrich_context(results)

    # 배치 메서드가 1회만 호출됐는지 확인
    mock_repo.find_chunks_by_ids.assert_called_once()
    # 단건 메서드는 호출되지 않아야 함
    mock_repo.find_chunk_by_id.assert_not_called()

    assert enriched[0]["parent_text"] == "부모 청크 텍스트"
    assert enriched[1]["parent_text"] == "부모 청크 텍스트"


@pytest.mark.asyncio
async def test_enrich_context_no_parent_ids():
    """parent_chunk_id 없는 결과 → DB 호출 없음, parent_text 빈 문자열."""
    mock_repo = AsyncMock()

    service = RagService(
        embedding_repo=mock_repo,
        embedding_service=AsyncMock(),
        ai_service=AsyncMock(),
    )

    results = [
        {"id": str(uuid.uuid4()), "chunk_text": "부모 없는 청크"},
    ]
    enriched = await service._enrich_context(results)

    mock_repo.find_chunks_by_ids.assert_not_called()
    assert enriched[0]["parent_text"] == ""


@pytest.mark.asyncio
async def test_enrich_context_empty_results():
    """빈 결과 → DB 미호출."""
    mock_repo = AsyncMock()

    service = RagService(
        embedding_repo=mock_repo,
        embedding_service=AsyncMock(),
        ai_service=AsyncMock(),
    )

    enriched = await service._enrich_context([])

    mock_repo.find_chunks_by_ids.assert_not_called()
    assert enriched == []
```

- [ ] **Step 2: 테스트 실행 — FAIL 확인** (아직 `_enrich_context`가 N+1을 사용하므로 실패)

```bash
cd backend && uv run pytest tests/rag/test_rag_service.py::test_enrich_context_uses_batch_query -v
```

Expected: FAIL — `assert_not_called()` on `find_chunk_by_id` 실패 또는 `find_chunks_by_ids` not called

- [ ] **Step 3: `rag/service.py`의 `_enrich_context` 배치로 교체**

`backend/src/rag/service.py`에서 `_enrich_context` 메서드 전체를 다음으로 교체한다.

```python
async def _enrich_context(self, results: list[dict]) -> list[dict]:
    """Level 2 검색 결과에 parent Level 1 컨텍스트 추가. 배치 쿼리 1회."""
    # uuid.UUID(str(...)) — DB에서 UUID 객체로 올 수도 있고 문자열로 올 수도 있어 str 경유
    parent_ids = [
        uuid.UUID(str(r["parent_chunk_id"]))
        for r in results if r.get("parent_chunk_id")
    ]
    parents = await self.embedding_repo.find_chunks_by_ids(parent_ids)
    enriched = []
    for r in results:
        parent_text = ""
        pid = r.get("parent_chunk_id")
        if pid:
            parent = parents.get(uuid.UUID(str(pid)))
            if parent:
                parent_text = parent.chunk_text
        enriched.append({**r, "parent_text": parent_text})
    return enriched
```

- [ ] **Step 4: 테스트 실행 — PASS 확인**

```bash
cd backend && uv run pytest tests/rag/test_rag_service.py -v
```

Expected: 6 passed (기존 3 + 신규 3)

- [ ] **Step 5: 커밋**

```bash
cd backend && git add src/embeddings/repository.py src/rag/service.py tests/rag/test_rag_service.py
git commit -m "feat(rag): BL-003 _enrich_context N+1 → find_chunks_by_ids 배치 쿼리"
```

---

## Task 5: 전체 테스트 회귀 확인

- [ ] **Step 1: 전체 단위 테스트 실행**

```bash
cd backend && uv run pytest tests/ -v --ignore=tests/projects/test_members_integration.py
```

Expected: 모든 테스트 PASS (통합 테스트는 TestContainers 필요로 제외)

- [ ] **Step 2: 통합 테스트 (선택 — Docker 필요)**

```bash
cd backend && uv run pytest tests/projects/test_members_integration.py -v -m integration
```

- [ ] **Step 3: 최종 git status 확인 — 미커밋 파일 없음**

```bash
git status
```

Expected: `nothing to commit, working tree clean`
