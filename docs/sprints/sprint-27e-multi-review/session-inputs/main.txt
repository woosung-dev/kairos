너는 Claude Code (Opus 4.7 또는 그 이상) 자동 에이전트다. Kairos 프로젝트의 production 배포 *전* 4-Reviewer Multi-Agent Audit (Sprint 27e) 를 진행한다.

[GOAL — Stop hook 자동 보장, 이 세션이 끝나기 전엔 종료 X]

`sprint-27e/multi-review` 신설 브랜치에서 4 전문 reviewer (보안 / 성능 / 테스트커버리지 / 아키텍처) 가 Personal + Team 2 시나리오를 모두 audit → 통합 보고서 작성 → 차단 결함 fix → 검증 → commit/push → PR 생성 → 종료 보고까지 모두 완료. 중간에 압축하거나 빼먹지 말고 끝까지 순차 진행.

[환경]

- 프로젝트 루트: /Users/woosung/project/agy-project/kairos
- main HEAD (Sprint 27d 완료): 1b24898 ("Sprint 27d pre-GA audit (3-세션 통합) + fix bundle — 최종 GO 8.02/10 (#108)")
- 현재 브랜치: main (clean) → 본 세션 시작 시 `git checkout -b sprint-27e/multi-review`
- FE: http://localhost:3000 (Next.js 16, 사용자 직접 띄움 — 이 세션은 띄우지 않음)
- BE: http://localhost:8000/api/v1 (FastAPI + uvicorn, 사용자 직접 띄움)
- Clerk dev creative-boxer-79.clerk.accounts.dev (Production 미발급, ADR-022 SKIP)
- GEMINI_API_KEY: set (Sprint 27c 에서 재발급, BL-S27c-2 closed)
- 로그인 계정 d@e.com (Personal workspace `e968c95f-4bbe-4f12-9468-2741c047e142` + Team `7f9f446d-9b7f-4ae7-aa9b-ac861fb81b11` "QA Cycle C Team")
- seed credentials ~/.kairos-qa-secrets/seed-credentials-2026-05-17.env (SENTINEL A/B/CASUAL/MOBILE/POWER, JWT expired → email/password 유효)
- production audit SKIP (사용자 정책)
- Sentry 의도적 SKIP (사용자 정책)
- 응답 언어: 한국어 (CLAUDE.md 글로벌, 코드 식별자만 영어)

[Sprint 27e 의 위치]

Sprint 27d 가 product/UX 시각 6 agent audit 이었으나 보안/성능/테스트커버리지/아키텍처 깊이 부족. 본 sprint 는 기술 deep audit. production 배포 (Cloud Run BE + Vercel FE) 또는 외부 5명 dogfooding 진입 *전* 최종 검증 루프.

[필독 산출물]

본 세션 시작 시 다음 모두 read:

1. **본 sprint 의 SCOPE**: `docs/sprints/sprint-27e-multi-review/SCOPE.md`
2. **4 reviewer 정의** (각 standalone):
   - `docs/sprints/sprint-27e-multi-review/agents/security-reviewer.md`
   - `docs/sprints/sprint-27e-multi-review/agents/performance-analyst.md`
   - `docs/sprints/sprint-27e-multi-review/agents/test-coverage-reviewer.md`
   - `docs/sprints/sprint-27e-multi-review/agents/architecture-guardian.md`
3. **Sprint 27d 결과** (baseline):
   - `docs/sprints/sprint-27d-pre-ga-audit/final-integrated-report.md`
   - `docs/sprints/sprint-27d-pre-ga-audit/codex/codex-adversarial.md` (이미 본 결과 — 중복 회피 가이드)
4. **Sprint 28 plan** (외부 5명 진입 후 sprint, carry 항목 인지): `docs/plans/active/sprint-28-dogfooding-stabilize.md`
5. **헌법**: `CONTEXT-MAP.md` + `docs/adr/` (특히 014/019/020/021/022/023/024)
6. **모듈별 CONTEXT.md**: BE 15 모듈 + FE 13 features 각 디렉토리의 CONTEXT.md

[Step 0 — 환경 검증 + 브랜치 생성 (5분)]

```bash
curl -s -o /dev/null -w "FE: %{http_code} " http://localhost:3000/
curl -s -o /dev/null -w "BE: %{http_code}\n" http://localhost:8000/api/v1/health
git status  # main clean 확인
git log --oneline -3  # 1b24898 까지 확인
git checkout -b sprint-27e/multi-review
mkdir -p docs/sprints/sprint-27e-multi-review/screenshots
```

둘 다 200 + main clean + 브랜치 생성 확인. 아니면 사용자에게 알림.

[Step 1 — 4 reviewer 순차 spawn (Agent tool, 각 ~30-45분, 병렬 권장)]

본 세션의 main orchestrator 는 다음 4 sub-agent 를 spawn. 동시 spawn 으로 병렬 처리 (단일 메시지 안에 4 Agent tool 호출).

각 sub-agent 의 prompt:
```
너는 Sprint 27e <reviewer 이름> 이다. `docs/sprints/sprint-27e-multi-review/agents/<reviewer-file>.md` 의 검사 항목 + 출력 형식 + 차단/비차단 분류 기준을 그대로 따른다.

검증 대상:
- main HEAD `1b24898` (Sprint 27d 완료 시점)
- Personal workspace `e968c95f-4bbe-4f12-9468-2741c047e142` + Team workspace `7f9f446d-9b7f-4ae7-aa9b-ac861fb81b11` 2 시나리오 모두
- 검사 항목 정의 파일의 모든 list 항목 빠짐없이

도구:
- Read / Grep / Bash (pytest / npm test / curl / find / 검색)
- MCP Playwright (가능 시 — 사용자 Chrome 점유 시 fail-graceful)
- 필요한 모든 sub-tool

출력:
- `docs/sprints/sprint-27e-multi-review/<reviewer>-findings.md` (정의 파일의 출력 형식 그대로)
- 발견사항 ID prefix: `BUG-S27e-<SEC/PERF/TEST/ARCH>-N`
- 차단/비차단 분류 명확

종료 조건:
- 정의 파일의 모든 검사 항목 cover
- 정량 baseline 측정 (해당 reviewer 의 경우)
- file:line + 권장 fix 명시
- summary 매트릭스 + 가장 critical 3건 강조

응답 길이 제한: 산출물 파일 작성으로 처리 (대화에 다 출력 X)
```

4 sub-agent 모두 종료 후 main orchestrator 가 산출물 4개 read.

[Step 2 — 통합 보고서 작성 (~30분)]

`docs/sprints/sprint-27e-multi-review/integrated-report.md`:

1. **Executive Summary** (1 페이지):
   - 4 reviewer 종합 verdict (GO / NEEDS-FIX / NO-GO)
   - 차단 결함 갯수 + 비차단 결함 갯수
   - 가장 critical finding 3건 강조 (file:line + 권장 fix + 영향도)
2. **중복 발견 사항 병합**: 같은 결함을 여러 reviewer 가 다른 시각으로 발견 시 → 단일 ID 통합 + 모든 시각 인용
3. **리뷰어 간 충돌 해결**: 보안 vs 성능 충돌 시 우선순위 = 보안 > 아키텍처 > 테스트 > 성능
4. **심각도 + 영향도 우선순위 매트릭스** (전체 발견사항):
   ```
   | ID | reviewer | 심각도 | 차단? | file:line | 영향도 | 권장 fix |
   ```
5. **차단 vs 비차단 분류 표시**: red / yellow 마크

`docs/sprints/sprint-27e-multi-review/pr-comment.md`:

GitHub PR 직접 첨부 형식. 차단 결함만 expanded, 비차단은 collapsed. 각 차단 결함마다:

```markdown
### 🔴 BUG-S27e-<X>-N — <한 줄 요약> (Blocking)

**file**: `path/to/file.py:line`
**reviewer**: <reviewer 이름>
**카테고리**: <OWASP / 영역 / 헌법 등>

```<lang>
# 현 코드
...
```

**권장 fix**:

```<lang>
# 수정 후
...
```

**영향도**: <누가 / 어떻게 / 어디까지>
```

`docs/sprints/sprint-27e-multi-review/report.html`:

Sprint 27d `report.html` style 차용 (dark mode + condition grid + bug card). 4 reviewer 결과 panel + 통합 verdict 큰 글씨.

[Step 3 — 차단 결함 사용자 결정 (블록킹 절차)]

차단 결함 0건이면 Step 4 스킵.
차단 결함 1건 이상이면:

1. 통합 보고서의 차단 결함 list 사용자에게 보고
2. AskUserQuestion 으로 옵션 제시:
   - (a) 본 sprint 안에서 모두 fix (autonomous)
   - (b) 선택 fix (사용자 P0 만 등)
   - (c) carry — production 진입 보류 + Sprint 28 으로 이월 (NO-GO)
3. 사용자 답변 후 진행

[Step 4 — 차단 결함 fix (사용자 승인 시, 각 결함 ~30-60분)]

각 차단 결함에 대해:

1. TaskCreate 로 추적
2. 권장 fix 코드 적용
3. 회귀 가드 (pytest / vitest / e2e spec 1건) 추가
4. 인접 test 재실행
5. 통합 보고서 status 갱신 (RESOLVED)

모든 fix 후:
- `cd backend && uv run pytest -x` 전체 PASS
- `cd frontend && npm run typecheck && npm test` 전체 PASS
- (가능 시) `pnpm e2e` focused 핵심 spec 재실행

[Step 5 — commit + push + PR 생성 (사용자 사전 승인)]

```bash
git add docs/sprints/sprint-27e-multi-review/ \
        <fix 적용된 파일들>

git commit -m "audit + fix: Sprint 27e 4-reviewer multi-agent audit + 차단 결함 N건 fix

(본문: 통합 verdict + 차단 결함 fix 상세 + BL-S27e-* 등재 + 검증 결과)

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"

git push origin sprint-27e/multi-review

gh pr create --title "Sprint 27e 4-reviewer multi-agent audit + 차단 결함 fix" --body "$(cat <<'EOF'
## Summary

- 4 전문 reviewer (보안 / 성능 / 테스트커버리지 / 아키텍처) audit
- Personal + Team 2 시나리오 모두 검증
- 차단 결함 N건 fix + 비차단 M건 BL carry
- production 진입 unlocking (GO 판정 시) 또는 NEEDS-FIX

(본문: 통합 verdict + 4 reviewer 매트릭스 요약 + 차단 결함 status + BL carry list)

## Test plan

- [x] backend pytest 전체 PASS
- [x] frontend typecheck 0
- [x] frontend vitest 전체 PASS
- [x] e2e 핵심 spec PASS
- [x] 보안 회귀 가드 신규 N건 추가
- [x] 4 reviewer 산출물 + 통합 보고서 + HTML 모두 산출

🤖 Generated with [Claude Code](https://claude.com/claude-code)
EOF
)"
```

[Step 6 — 통합 검증 (10분)]

PR 생성 후:
- `gh pr checks <number>` CI 결과 monitor (Sprint 27d 패턴 따라 Monitor tool 활용)
- 모두 PASS 확인
- 사용자에게 merge 결정 요청

[도구]

- Bash (curl, git, gh, pytest, npm)
- Read / Write / Edit / Grep / Glob
- Agent (Explore / Plan / general-purpose — 4 sub-reviewer spawn)
- MCP Playwright (browser_navigate / browser_console_messages / browser_evaluate 등 — 사용자 Chrome 점유 시 fail-graceful + 사용자에게 알림)
- TaskCreate / TaskUpdate (각 step + 각 차단 결함 fix 추적)
- Monitor (CI 결과)

[종료 보고]

- 4 reviewer audit 완료 (각 산출물 hash)
- 통합 보고서 verdict (GO / NEEDS-FIX / NO-GO)
- 차단 결함 N건 fix status
- 비차단 결함 M건 BL-S27e-5~* 등재
- PR 생성 + CI 결과
- 다음 단계 (사용자 액션):
  - GO: PR merge → production 진입 또는 외부 5명 dogfooding 진입
  - NEEDS-FIX 추가 cycle: 본 sprint 안에서 또는 Sprint 28 carry
  - NO-GO: production 진입 보류 + 근본 원인 sprint 별도

[정책 SKIP]

- production 환경 실 audit (실 Cloud Run / Vercel prod): SKIP (사용자 정책)
- Sentry 활성 결정: SKIP 유지 (단 보안 reviewer 가 "Sentry 없이 외부 진입 가능한지" 1건 평가는 진행)
- Clerk Production 키 발급: SKIP 유지 (ADR-022)

[시작 신호]

필독 산출물 1~6 모두 read 후 Step 0 환경 검증부터 시작. 모든 Step 끝까지 순차 진행. 중간 break X. 사용자 확인 없이 자동 진행 (Git Safety Protocol 의 commit/push 도 본 goal 안에 사전 승인됨 — 단 Step 3 의 차단 결함 fix 결정은 사용자 답변 필요).
